# Slider plugin for OctoberCMS

### Manual Instalation
Insert the plugin into the folder **plugin** in you OctoberCMS project and install via command line using:

```
php artisan plugin:install MmatovskiSlider.PluginName
```